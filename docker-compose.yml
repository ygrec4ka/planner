services:
  app:
    build:
      context: ./
      dockerfile: ./Dockerfile
    environment:
      - APP_CONFIG__DB__URL=postgresql+asyncpg://postgres:postgres@pg:5432/planner
    ports:
      - "5050:8000"
    depends_on:
      pg:
        condition: service_healthy
    develop:
      watch:
       - action: sync+restart
         path: ./todolist
         target: /app

  pg:
    image: postgres:18
    environment:
      POSTGRES_DB: planner
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /var/lib/postgresql/data
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d planner -t 5"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.org
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "8000:80"

  smtp:
    image: maildev/maildev
    container_name: todo_smtp
    ports:
      - "1025:1025"
      - "1080:1080"

volumes:
  pgdata: